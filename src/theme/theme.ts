import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#324b4c",
    },
    secondary: {
      main: "#e3feff",
    },
  },
});

export default theme;
